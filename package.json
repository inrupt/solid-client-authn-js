{
  "name": "solid-authenticator-root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean --stream",
    "build": "lerna run build --stream",
    "pub": "lerna publish",
    "lint": "lerna run lint --stream",
    "test": "concurrently \"lerna run test-server --stream\" \"lerna run build-core --stream && lerna run test --stream\"",
    "unit-test": "lerna run build-core --stream && lerna run unit-test --stream",
    "acceptance-test": "concurrently \"lerna run test-server --stream\" \"lerna run build-core --stream && lerna run browser-acceptance-test --stream\"",
    "core-test": "lerna run core-test --stream",
    "core-unit-test": "lerna run core-unit-test --stream",
    "browser-dev": "concurrently \"lerna run test-server-dev --stream\" \"lerna run build-core-watch --stream\" \"lerna run browser-dev --stream\"",
    "browser-test": "lerna run build-core --stream && lerna run browser-test --stream",
    "browser-unit-test": "lerna run build-core --stream && lerna run browser-unit-test --stream",
    "browser-acceptance-test": "concurrently \"lerna run test-server --stream\" \"lerna run build-core --stream && lerna run browser-acceptance-test --stream\"",
    "node-test": "lerna run build-core --stream && lerna run node-test --stream",
    "node-unit-test": "lerna run build-core --stream && lerna run node-unit-test --stream",
    "node-acceptance-test": "concurrently \"lerna run test-server --stream\" \"lerna run build-core --stream && lerna run node-acceptance-test --stream\""
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@typescript-eslint/eslint-plugin": "^2.20.0",
    "@typescript-eslint/parser": "^2.20.0",
    "concurrently": "^5.0.2",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-config-standard-with-typescript": "^14.0.0",
    "eslint-plugin-import": "^2.20.1",
    "eslint-plugin-node": "^11.0.0",
    "eslint-plugin-prettier": "^3.1.2",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1",
    "jest": "^24.9.0",
    "lerna": "~3.14.0",
    "prettier": "^1.19.1",
    "typescript": "^3.6.4"
  }
}
