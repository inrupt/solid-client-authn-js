{
  "name": "solid-authenticator-root",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "@types/node": "^12.12.19",
    "lerna": "~3.14.0",
    "tslint": "^5.20.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "~3.4.5"
  }
}
