{
  "name": "@solid/authenticator-core",
  "version": "0.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "build-core": "npm run build",
    "build-core-watch": "tsc-watch --preserveWatchOutput -p tsconfig.build.json",
    "lint": "tslint -t stylish --project \"tsconfig.json\"",
    "test": "npm run lint && npm run unit-test",
    "unit-test": "npm run core-unit-test",
    "core-test": "npm run test",
    "core-unit-test": "jest --coverage --verbose",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.24",
    "jest": "^24.9.0",
    "tsc-watch": "^4.0.0",
    "tslint": "^5.20.1",
    "tslint-config-standard": "^9.0.0",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "@types/url-parse": "^1.4.3",
    "ajv": "^6.10.2",
    "body-parser": "^1.19.0",
    "cross-fetch": "^3.0.4",
    "jose": "^1.19.0",
    "node-fetch": "^2.6.0",
    "reflect-metadata": "^0.1.13",
    "tsyringe": "^4.0.1",
    "url": "^0.11.0",
    "url-parse": "^1.4.7"
  }
}
