import validateSchema from '../../src/util/validateSchema'

describe('validateSchema', () => {
  it('validates a simple schema', () = {

  })

  it('throws an error when the schema throw error is on', () => {

  })

  it('does not throw and error when throw error is not on', () => {
    
  })
})
